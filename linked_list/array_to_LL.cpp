// in this question we have to convert the arry to linked list 
/*
Node* constructLL(vector<int>& arr) {
    Node* head=new Node(arr[0]);
    Node* tail=head;
    for(int i=1 ;i<arr.size() ;i++){
        Node* temp=new Node(arr[i]);
        tail->next=temp;
        tail=tail->next;

    }

    return head ;
}
*/