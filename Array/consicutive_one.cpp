// in this problem we have to find the maximum number of  consicutive  ones 
/*
int consecutiveOnes(vector<int>& arr){
    //Write your code here.
    int maxi=0;
    int count=0;
    int n=arr.size();

    for(int i=0 ;i<n ;i++){
       if(arr[i]==1){
           count++;
           maxi=max(maxi,count);
       }
       else{
           count=0;
       }
    }

    return maxi;
*/